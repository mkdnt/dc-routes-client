import React, { Component } from 'react'

export class Profile extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default Profile

//needs to go to NavBar and be there if user is logged in; 